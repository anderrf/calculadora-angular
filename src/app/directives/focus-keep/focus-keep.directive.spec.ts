import { FocusKeepDirective } from './focus-keep.directive';

describe('FocusKeepDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusKeepDirective();
    expect(directive).toBeTruthy();
  });
});
